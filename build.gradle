apply plugin: 'java'
apply plugin: 'maven'

group = "jupyter"
version = '0.2.1'

repositories {
  mavenCentral()
}

ext {
  scalaVersion = '2.11'
}

sourceCompatibility = '1.8'
targetCompatibility = '1.8'

dependencies {
  testCompile 'junit:junit:4.12'
}
