apply plugin: 'java'
apply plugin: 'maven'

group = "jupyter"
version = '0.1.0'

repositories {
  mavenCentral()
}

ext {
  scalaVersion = '2.11'
}

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

dependencies {
  testCompile 'junit:junit:4.12'
}
